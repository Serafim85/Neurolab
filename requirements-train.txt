# Neurolab Tiny LoRA train stack (Python 3.10+)
# Install into a venv: python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-train.txt
#
# Apple Silicon: PyTorch MPS works for light LoRA; CUDA preferred if available.
# Unsloth (CUDA-only) is optional — see docs/TRAIN-TINY-LORA.md

torch>=2.2.0
transformers>=4.44.0
datasets>=2.19.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.0
sentencepiece>=0.2.0
protobuf>=4.25.0
safetensors>=0.4.0
huggingface_hub>=0.24.0
