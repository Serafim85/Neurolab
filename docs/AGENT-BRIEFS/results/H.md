# Result H — CLAIMS.md + генератор паспорта

**Track:** H (волна 2, 2026-08-08) · защита от переобещания и от расхождения паспорта с артефактом
**Статус:** done · git не трогал (мержит оркестратор)

---

## 1. Расхождения «записано руками» vs «показывает диск / отчёты»

Ровно то, ради чего нужен генератор. Всё найдено машинно, кроме H-07/H-08.

| # | Записано руками | Что на самом деле | Где видно |
|---|---|---|---|
| **H-01** | В `CARD.md` не было **ни SHA256, ни LICENSE, ни даты** | `AGENTS.md` §5.7 и `GOALS.md` §4 требуют все три в паспорте. Паспорт был неполон с самого начала | старый `CARD.md` |
| **H-02** | Ладдер перечисляет `hammer` (15/20) и `hammer2` (17/20) как две модели | `outpost-tiny-hammer.Q4_K_M.gguf` и `outpost-tiny-hammer2.Q4_K_M.gguf` **побайтно одинаковы**, обе `3a7129549bf19c69…6e8c`. Артефакта, который дал 15/20, на диске нет — файл перезаписан. У копии `hammer2` вдобавок нет sidecar SHA | секция «Byte-identical artifacts» в новом `CARD.md` |
| **H-03** | «Best lab GGUF alone · 17/20» стояло **второй** строкой, а первой — «Demo / eval bar 20/20» | Паспорт модели открывался цифрой продукта, а не модели. Теперь обе цифры в одной таблице, разными строками, с колонкой «сколько id закрыл runtime» | `CARD.md` § Eval |
| **H-04** | Отчёт `tiny-hammer2-plus-guard.md` подаёт `20/20` как результат прогона | Машинный подсчёт по колонке Source того же отчёта: **7 model / 3 runtime из 10**. Цифра теперь считается скриптом, а не переписывается | `CARD.md` § Eval, колонка «Per-id source» |
| **H-05** | «Guard закрыл три id» (`ru_refuse_cloud`, `contour_clarify`, `ru_formal`) | Арифметика 17 → 20 закрывается двумя: clarify 0→2 и refuse 1→2. В таблице `tiny-micro-vs-hammer2.md` у hammer2 `ru_formal` уже стоит **2** — вклад третьего id guard'а относительно 17/20 равен **нулю** | CLAIMS C-03 |
| **H-06** | Отчёты ссылаются на сырые прогоны | Каталог сырых ответов прогона 20/20 — `eval/results/raw/baseline-20260721-170217/` — **пустой**, а сам отчёт поля `Raw` не имеет вовсе. Плюс `eval/results/raw/` в `.gitignore`. Флагманскую цифру сегодня нечем перепроверить | CLAIMS C-05; колонка «Raw evidence» в `CARD.md` |
| **H-07** | `tiny-micro-vs-hammer2.md` даёт колонку hammer2 и поле `Raw: baseline-20260721-000725` | `meta.txt` этого каталога называет GGUF **micro**, не hammer2. Колонка hammer2 там — перенесённый baseline без собственного прогона на диске | `eval/results/raw/baseline-20260721-000725/meta.txt` |
| **H-08** | `ru_formal` у hammer2 = 2 | В единственном подтверждённом hammer2-прогоне того же дня (`raw/baseline-20260721-163032`, `meta.txt` = hammer2) `ru_formal` — **одно** предложение и со смыслом наоборот («исключая возможность контроля со стороны ИБ»), то есть 1. Один GGUF, два разных записанных балла | CLAIMS C-04 |
| **H-09** | В `CARD.md` перечислено 8 GGUF | На диске 12 `Q4_K_M` артефактов линейки: не были перечислены `micro`, `diverse`, `evalgold` и копия `hammer2` | `CARD.md` § Artifacts on disk |
| **H-10** | Ни один отчёт не сообщает, как получен балл | `eval/README.md`: «Пока без автоматического scorer». Все `N/20` — ручная оценка, один прогон, N=10, temp 0.2. Теперь это написано прямо под таблицей баллов | `CARD.md` § Eval, § Limits |
| **H-11** | Upstream LICENSE базовой модели | В репозитории её **нет нигде**: ни `artifacts/base/LICENSE`, ни docs. Скрипт пишет `MISSING` и явно запрещает додумывать Apache-2.0 (Qwen2.5-3B — не Apache) | `CARD.md` § Base |

Совпало с диском без расхождений: все 11 sidecar-SHA (`match`), SHA базовой модели,
размеры. То есть врал не учёт хешей, а то, чего в паспорте не было вообще.

---

## 2. ⚠️ Цифры, которые уже цитируются наружу без оговорки

**Это главная находка трека.** Файл `docs/INVESTOR-NORTH-STAR.md` — по собственной
шапке «Investor / grant one-pager» и «only claims backed by STATUS Done». Именно
из него будут копировать в дек и в заявку. Секция **§ Proof points (today)**
содержит четыре строки, и **ни одна не несёт оговорки**:

| Строка в `INVESTOR-NORTH-STAR.md` | Чего не хватает | Правильная формулировка |
|---|---|---|
| «Pilot contour chat: **hammer2 + contour_guard · 20/20**» | что 3 из 10 id закрывает canned-ответ runtime, а не сеть; что лист внутренний и оценён вручную | CLAIMS **C-02** |
| «Closed Sandbox stress+speed f1 **≈0.93**» | разброс `± 0.04`, число seed, слово «синтетическая». Взято лучшее из двух чисел: hardening-прогон даёт `0.90 ± 0.06` с минимумом 0.76 | CLAIMS **C-20** + **C-21** вместе |
| «Synapse specialist class ≈ **0.86**» | что это замороженная фикстура одного прогона; что oracle-потолок 0.8902 не закрыт (разрыв ~2.7 pp); что прирост над stub = +0.75 pp | CLAIMS **C-40** |
| «Agent-eval live **20/20** with format runtime» | читается как результат модели: модель сама даёт 17/20, 3 из 10 id выдаёт runtime-форматтер, guard при этом выключен — это **не** конфигурация пилота | CLAIMS **C-11** (и C-10 рядом) |

Плюс там же таблица Product line: «Outpost-Tiny (≈3B) · Contour chat, pilot bar
**20/20** with guard» — guard упомянут, доля runtime нет.

Тот же голый `20/20` без доли runtime: `docs/INDEX.md` строка 16 и шапка
`STATUS.md` (оба вне моих файлов — правки предлагаю ниже).

**Смягчающее обстоятельство:** внутри репозитория честность есть —
`PILOT-CONTOUR-CHAT.md` §5 «Guard vs model (honest)» и §6 «Not claimed»
раскрывают всё. Проблема ровно в том, что наружу смотрит другой файл.

**Запрос оркестратору:** переписать § Proof points в `INVESTOR-NORTH-STAR.md`
через C-02 / C-20+C-21 / C-40 / C-11 и добавить в шапку строку
«цитируемые цифры — только из `docs/CLAIMS.md`». Файл не мой, не трогал.

---

## 3. Предложенная строка в `STATUS.md` Session log

Вставить в начало § Session log (файл не трогал):

```markdown
### 2026-08-08 — CLAIMS registry + генератор паспорта (трек H)

- `docs/CLAIMS.md` — единственный источник цитируемых наружу цифр: заявление /
  источник / оговорка / `public`–`internal`. Модель и модель+runtime всегда
  двумя строками; §7 «заявлять нельзя» сведён из существующих non-goals
- `scripts/gen_model_card.py` — машинный блок паспорта между маркерами: SHA256
  с диска, размеры, сверка с sidecar, баллы из `eval/results/` раздельно
  model / model+runtime, наличие сырых данных. stdlib, без сети, идемпотентно;
  `--check` для шлюза, `MISSING` вместо падения при отсутствии GGUF
- Найдено: `hammer` и `hammer2` — **один и тот же файл** побайтно; каталог
  сырых ответов прогона 20/20 **пуст**; `ru_formal` у одного GGUF записан и как
  2, и как 1; upstream LICENSE в репозитории отсутствует
- **Наружу без оговорки:** § Proof points в `INVESTOR-NORTH-STAR.md` — четыре
  цифры, включая `20/20` без доли runtime и `f1 ≈0.93` без разброса
- Verify: `python3 scripts/gen_model_card.py --check`
```

## 4. Предложенная строка в `docs/INDEX.md`

Поставить сразу после строки `../STATUS.md`, до `AGENT-BRIEFS/README.md`
(файл не трогал):

```markdown
| [`CLAIMS.md`](CLAIMS.md) | **Реестр цитируемых цифр** — единственный источник для грантов / инвесторов / SOW: заявление + оговорка + `public`/`internal` |
```

---

## 5. Команда проверки

```bash
python3 scripts/gen_model_card.py --check
```

Ожидается `models/outpost-tiny/CARD.md: up to date`, код возврата 0. Полный
цикл: `python3 scripts/gen_model_card.py && python3 scripts/gen_model_card.py --check`
(первый прогон читает ~23 ГБ GGUF: ≈20 с на прогретом кеше, ≈2 мин на холодном;
`--skip-hash` — структура без чтения байтов).

---

## 6. Изменённые файлы

| Файл | Что |
|---|---|
| `docs/CLAIMS.md` | **new** — 8 секций, 28 строк-заявлений, разрешённые абзацы RU/EN, §7 «заявлять нельзя», чек-лист перед отправкой |
| `scripts/gen_model_card.py` | **new** — генератор машинного блока паспорта (381 строка, stdlib) |
| `models/outpost-tiny/CARD.md` | **rewritten** — ручные секции (назначение, ограничения, рецепт) + генерируемый блок между маркерами |
| `docs/AGENT-BRIEFS/results/H.md` | **new** — этот файл |

За пределы списка «Твои файлы» не выходил. Git-команд не выполнял.

---

## 7. Что требует решения человека

| # | Вопрос | Почему нельзя решить агенту |
|---|---|---|
| **1** | **Upstream LICENSE базовой модели.** В репозитории её текста нет нигде. Отдельно проверить по карточке upstream: у Qwen2.5 не все размеры под Apache-2.0, и 3B — как раз исключение. Пока лицензия не зафиксирована, распространять веса за пределы лаборатории нельзя | Юридический факт, догадка недопустима. Скрипт пишет `MISSING` и не подставляет Apache-2.0 |
| **2** | **Переписать § Proof points в `INVESTOR-NORTH-STAR.md`** по §2 выше | Не мой файл; и это GTM-формулировки — `AGENTS.md` §9 требует человека |
| **3** | **Дубль `outpost-tiny-hammer2.Q4_K_M.gguf`** — удалить копию или зафиксировать как осознанный алиас (плюс sidecar SHA) | 1.8 ГБ артефакта; удаление данных |
| **4** | **Перепрогнать лист 20/20 с сохранением сырых ответов** — иначе C-02 остаётся неверифицируемым, а на нём стоит весь пилотный оффер | Нужен Metal/GPU-хост и Commercial-бинарник; пересекается с треком E (скорер) и E/`.gitignore` для raw |
| **5** | **Зафиксировать правило:** наружу цитируем только из `CLAIMS.md`. Кандидат в ADR — предлагаю текстом, `DECISIONS.md` в волне никто не пишет | Правило процесса, не код |

Предлагаемый ADR (оркестратору, если человек согласен):

> **NL-ADR-0xx — CLAIMS.md как единственный источник внешних цифр.**
> Контекст: честные оговорки существуют, но рассыпаны по отчётам, а наружу
> смотрит `INVESTOR-NORTH-STAR.md`, где цифры стоят голыми.
> Решение: любое число в гранте / SOW / деке / публикации цитируется дословно
> из `docs/CLAIMS.md` вместе с его оговоркой; уровень `internal` наружу не идёт;
> модель и модель+runtime никогда не сливаются в одно число.
> Следствие: новый измеренный результат → строка в `CLAIMS.md` в той же сессии,
> иначе он не цитируем. Машинная часть паспорта — только из
> `scripts/gen_model_card.py`.
