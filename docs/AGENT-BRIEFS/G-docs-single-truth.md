# Brief G — One source of truth + STATUS rotation

**Track:** Убрать расхождения в документах, которые агенты читают первыми
**Primary repo:** `/Users/valentin/Projects/neurolab`
**Read first:** `AGENTS.md` (весь) · `STATUS.md` · `docs/ARCHITECTURE.md` §6 · `docs/CLOSED-SANDBOX-VERIFY.md` · `docs/CLOSED-SANDBOX-MVP.md` §9
**Wave:** 2026-08-08 (E–I) · **ты единственный владелец `STATUS.md`**

---

## Зачем этот бриф

Найдено аудитом 2026-08-08:

1. **Фокус описан в трёх местах, и два врут.** `AGENTS.md` §6 «Текущий фокус»
   говорит про Tiny LoRA и «качество vs baseline **14/16**» — шкала /16 давно не
   используется. `ARCHITECTURE.md` §6 «Current focus» — «Tiny baseline + LoRA на
   3B», «Mid/Large — после Tiny quality bar». А `STATUS.md` говорит «Pause Tiny
   LoRA sheet chase», и фокус фактически на Closed Sandbox. При этом шаг 3 ритуала
   отправляет агента именно в `ARCHITECTURE.md` §6.
2. **Листы верификации отстали.** `CLOSED-SANDBOX-VERIFY.md` и MVP §9 говорят
   «**11 passed**». Фактически **51**. Документ, у которого единственная работа —
   фиксировать проверку, отстал на 11 дней активной разработки.
3. **`STATUS.md` — 423 строки и 58 записей журнала.** Он читается каждую сессию;
   рост дописыванием жжёт контекст и хоронит актуальное состояние под историей.

---

## Do exactly

### 1. Один источник правды о фокусе

`AGENTS.md` §6 и `ARCHITECTURE.md` §6 — **заменить дубликат на указатель** на
`STATUS.md` §Summary + §Next. Оставь в каждом месте 1–3 строки смысла («где
искать» + «что не сейчас» как принцип), но ни одной конкретной цифры и ни одного
конкретного текущего фокуса, который придётся синхронизировать вручную.

Заодно убери упоминание шкалы `14/16` — она мертва, актуальная шкала `/20`.

**Не тронь** §4 «Ритуал сессии» в `AGENTS.md`: пункт 3 «Закоммитить и запушить»
добавлен намеренно 2026-08-08, он нужен. Если ритуал ссылается на
`ARCHITECTURE.md` §6, поправь ссылку так, чтобы она вела туда, где правда.

### 2. Освежить листы верификации

Прогони сам и впиши фактические числа:

```bash
cd sandbox && PYTHONPATH=src python -m pytest -q -m "not integration"
```

Обнови `docs/CLOSED-SANDBOX-VERIFY.md` (новая строка в таблице с датой
2026-08-08) и `docs/CLOSED-SANDBOX-MVP.md` §9 «Verified». Старые строки не
удаляй — это история проверок, она ценна.

Integration-набор (3 теста) требует Metal/GPU и Commercial release-бинарь —
если не запускается, так и напиши, не выдумывай число.

### 3. Ротация журнала сессий

Создай `docs/SESSIONS-2026-07.md`. Перенеси туда записи журнала **старше
2026-08-01**, в том же порядке и без переписывания текста. В `STATUS.md` оставь:

- Summary / Ladder / Next как есть
- Session log: только записи с 2026-08-01
- строку-указатель на архив

Задача — чтобы `STATUS.md` стал заметно короче 423 строк при нулевой потере
информации.

### 4. INDEX

Ты владелец `docs/INDEX.md`. Впиши новый `SESSIONS-2026-07.md`. Другие агенты
этой волны создают файлы и **предлагают** строку в INDEX в своих результатах —
если их результаты уже лежат в `docs/AGENT-BRIEFS/results/`, забери строки
оттуда. Если их ещё нет, не жди: оркестратор домержит.

---

## Твои файлы (не выходи за них)

```text
STATUS.md                      AGENTS.md
docs/ARCHITECTURE.md           docs/INDEX.md
docs/CLOSED-SANDBOX-VERIFY.md  docs/CLOSED-SANDBOX-MVP.md
docs/SESSIONS-2026-07.md       .cursor/rules/00-neurolab.mdc
```

Читать можно всё. **Писать — только в этот список.**

---

## Forbidden

- Любые `git` команды. Мержит оркестратор
- Править `docs/DECISIONS.md` — ADR вставляет оркестратор
- Трогать `sandbox/`, `eval/`, `scripts/`, `.gitignore`, `.github/` — это брифы E / F / I
- Трогать `models/outpost-tiny/CARD.md` и `docs/CLAIMS.md` — это бриф H
- Удалять записи журнала: только перенос
- Ослаблять пункт «закоммитить и запушить» в ритуале
- Менять смысл ADR-решений при переформулировке — только устранение дублей и мёртвых цифр

---

## Definition of Done

- [ ] `AGENTS.md` §6 и `ARCHITECTURE.md` §6 — указатели, без дублирующих цифр
- [ ] Шкала `14/16` не встречается в репозитории как «текущая»
- [ ] VERIFY + MVP §9 содержат фактические числа с датой 2026-08-08
- [ ] `STATUS.md` короче, история целиком в `docs/SESSIONS-2026-07.md`
- [ ] `docs/INDEX.md` обновлён
- [ ] `docs/AGENT-BRIEFS/results/G.md` написан

---

## Result file → `docs/AGENT-BRIEFS/results/G.md`

Обязательно включи:

1. Было / стало по числу строк `STATUS.md`
2. Список всех мест, где нашлись расхождения (возможно, найдёшь больше трёх — доложи все)
3. Фактические числа тестов, которые вписал, и запускал ли integration
4. **Предложенную строку в STATUS Session log** — впиши её сам, ты владелец файла
5. Список изменённых файлов
