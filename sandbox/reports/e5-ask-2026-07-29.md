# E5 ask session — Closed Sandbox × Neurolab (step A)

**Date:** 2026-07-29  
**DoD track:** Ask по E5-метрикам (fixture, не live torch)  
**Raw turns:** [`e5-ask-session.json`](e5-ask-session.json)

## Setup

| Item | Value |
|---|---|
| Import | `examples/synapse_e5_import/` → fixture `e5-official.json` |
| Metrics | accuracy **0.856**, escalate_rate **0.068**, oracle_accuracy **0.89**, latency_proxy_ms **0.44** |
| Outpost | `sovereignd` + `config/sovereign.sandbox-ask.toml` · `:8098` |
| Model | `outpost-tiny-hammer` + contour_guard |
| Provider | `local` only |

```bash
# boot (if needed)
nohup ~/Projects/AI-Platform-Vision/target/release/sovereignd \
  ~/Projects/neurolab/config/sovereign.sandbox-ask.toml \
  >/tmp/sandbox-ask-e5.log 2>&1 &

cd ~/Projects/neurolab/sandbox
PYTHONPATH=src python -m closed_sandbox.cli run examples/synapse_e5_import/project.toml
PYTHONPATH=src python -m closed_sandbox.cli ask examples/synapse_e5_import/project.toml \
  "Ваш вопрос по метрикам"
```

`[contour].base_url` в примере выставлен на `http://127.0.0.1:8098/v1`.

## Turns (hammer2)

### Q1 — зачем escalate ~7%, а не всегда мозг?

**Model:** минимизация вызовов / лимит уточнения (формулировки ещё путают SNN ↔ brain).

**Engineer verdict:** ✅ направление верное (дорого звать мозг на 100%).  
Истина по канону: cascade закрывает ~93% локально; **escalate = вызов внешнего LLM**, не «ещё один SNN». Выгода = latency/cost × редкие uncertain rows; oracle показывает потолок **+3.4 pp** если мозг идеален на escalate.

### Q2 — oracle_accuracy 0.89 vs 0.856

**Model:** верхняя оценка, не product KPI.

**Engineer verdict:** ✅ верно (CARD / SYN-ADR-008).

### Q3 — budget_ok / spike_count=0 / synops=0

**Model:** host wrap, нет live SNN budget — нормально.

**Engineer verdict:** ✅ верно. Нули не значат «сеть пустая», а что pack — **host escalate gate**, не spike-engine внутри sandbox.

## Gaps (модель)

- Tiny всё ещё иногда путает **escalate → brain** с SNN (Q1).  
- Mitigation: domain-aware system prompt в `contour_ask.py` для `synapse_import` (уже добавлен); дальше — кусок в agent/LoRA data или Mid.

## Code tweaks this step

- `examples/synapse_e5_import/project.toml` → `base_url` `:8098`  
- `contour_ask.py` → system prompt для `synapse_import` (escalate / oracle / host-wrap zeros)

## DoD checklist (A)

- [x] Fixture run → metrics.json  
- [x] Outpost local ask ×3  
- [x] Report + engineer verdict  
- [ ] Live Synapse torch export (step **B** — not this note)

## Next

1. Step **B**: torch env → bench → overwrite fixture → `diff` + optional re-ask.  
2. Optional: 3–5 messages in `tiny-lora-agent` про escalate≠SNN.
